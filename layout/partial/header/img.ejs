<% if (theme.image.header_images && theme.image.header_images.length) { %>
<div id="imgs" class="pjax">
  <ul>
    <% theme.image.header_images.forEach(function(img) { %>
      <li class="item" data-background-image="<%= theme.image.host %><%= img %>"></li>
    <% }); %>
  </ul>
</div>
<% } %>
