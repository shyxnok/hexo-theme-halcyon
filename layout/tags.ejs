<div class="tags wrap">
  <h2 class="divider"><%= __('tags') %></h2>
  <% if (site.tags.length) { %>
    <div class="tag-cloud">
      <%- tagcloud({min_font: 14, max_font: 28, unit: 'px'}) %>
    </div>
  <% } else { %>
    <p class="empty"><%= __('no_tags') %></p>
  <% } %>
</div>
