<div class="categories wrap">
  <h2 class="divider"><%= __('categories') %></h2>
  <% if (site.categories.length) { %>
    <div class="category-list">
      <%- list_categories({show_count: true, depth: 0}) %>
    </div>
  <% } else { %>
    <p class="empty"><%= __('no_categories') %></p>
  <% } %>
</div>
